
  redis-fpc:
      image: redis:${REDIS_FPC_VERSION}
      ports:
        - "${REDIS_FPC_BINDED_PORT}:6379"
      volumes:
        - "./.warp/docker/volumes/redis-fpc:/data"
      #  - ${REDIS_FPC_CONF}:/usr/local/etc/redis/redis.conf
      #command: ["redis-server", "/usr/local/etc/redis/redis.conf"]
      networks:
        - back
